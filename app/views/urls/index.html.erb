<table>
<tr>
<th>Long URL</th>
<th>Short URL</th>
</tr>

<%  @urls.each do |url| %>
    <tr>
    <td> <%= url.long_url %></td>
    <td> <%= url.short_url %> </td>  
    </tr>  

<% end %>

</table>
<br/>

<%= link_to "Shorten Url!", new_url_path %>
